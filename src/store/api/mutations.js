const mutations = {
    SET_JAVA_TOKEN(state, token) {
        state.javaToken = token;
    },

    SET_AVATAR_TOKEN(state, token) {
        state.avatarToken = token
    }
}

export default mutations